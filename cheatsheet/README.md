# AWS CloudTrail Incident Responder Cheatsheet

Fast lookup of suspicious AWS CloudTrail activity, organized by MITRE ATT&CK tactic. Each row links to a ready-to-use [Sigma rule](../rules/). This file is generated from the rules by `scripts/build_docs.py` — do not edit by hand.

> **Tip:** the highest-signal fields to pivot on for almost any CloudTrail event are `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `eventName`, and `awsRegion`.

> **Deploy tiers:** **alert** (28) = high-signal, page on sight · **hunt** (70) = high-volume / context-dependent — run through the [enrichment + 90-day baseline layer](../docs/enrichment-and-baselining.md) or use for threat hunting, not direct paging.

**Jump to:** [Initial Access](#initial-access) · [Execution](#execution) · [Persistence](#persistence) · [Privilege Escalation](#privilege-escalation) · [Defense Evasion](#defense-evasion) · [Credential Access](#credential-access) · [Discovery](#discovery) · [Lateral Movement](#lateral-movement) · [Collection](#collection) · [Exfiltration](#exfiltration) · [Impact](#impact)


## Initial Access

ATT&CK tactic: [TA0001](https://attack.mitre.org/tactics/TA0001/) — 9 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| hunt | Organizations Handshake Accepted | [T1199](https://attack.mitre.org/techniques/T1199/) | `AcceptHandshake` | Detects acceptance of an AWS Organizations handshake (AcceptHandshake), e.g. joining/inviting an account. Abuse can establish a trusted relationship for… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Planned organization changes by cloud/platform teams. | [`accept-organization-handshake.yml`](../rules/initial-access/accept-organization-handshake.yml) |
| **alert** | Console Login Brute Force From Single Source IP | [T1110](https://attack.mitre.org/techniques/T1110/) | `ConsoleLogin` | Detects 5 or more failed console logins from a single source IP within 10 minutes, indicating brute-force/credential-stuffing against the AWS console. | `eventSource`, `responseElements.ConsoleLogin`, `sourceIPAddress`, `userIdentity.arn`, `userAgent`, `awsRegion` | Correlation: ≥5 events / 10m per `sourceIPAddress`. Level: **high**. | [`brute-force-console-login.yml`](../rules/initial-access/brute-force-console-login.yml) |
| hunt | Console Login Without MFA | [T1078.004](https://attack.mitre.org/techniques/T1078/004/) | `ConsoleLogin` | Detects a successful console sign-in where MFA was not used (additionalEventData.MFAUsed=No). For IAM users this is a control gap; for SSO/federated logins MFA… | `eventSource`, `responseElements.ConsoleLogin`, `additionalEventData.MFAUsed`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Federated/SSO logins where MFA is enforced at the IdP and reported as No.; Service or break-glass accounts… | [`console-login-without-mfa.yml`](../rules/initial-access/console-login-without-mfa.yml) |
| hunt | Management Console Login | [T1078.004](https://attack.mitre.org/techniques/T1078/004/) | `ConsoleLogin` | Detects an interactive sign-in to the AWS Management Console. Useful as a building block and for baselining who logs in, from where, and when. Tune with… | `eventSource`, `responseElements.ConsoleLogin`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Routine user and administrator console logins are expected. Baseline per principal and source IP/geo. | [`console-login.yml`](../rules/initial-access/console-login.yml) |
| hunt | Federation GetSigninToken | [T1550.001](https://attack.mitre.org/techniques/T1550/001/), [T1078.004](https://attack.mitre.org/techniques/T1078/004/) | `GetSigninToken` | Detects use of GetSigninToken, which exchanges temporary credentials for a console sign-in token (federated console access). Attackers with stolen temporary… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate SSO/federation brokers and identity tooling. | [`get-signin-token.yml`](../rules/initial-access/get-signin-token.yml) |
| hunt | Console Password Recovery Requested | [T1078.004](https://attack.mitre.org/techniques/T1078/004/), [T1098](https://attack.mitre.org/techniques/T1098/) | `PasswordRecoveryRequested` | Detects a request to recover/reset an AWS console password (PasswordRecoveryRequested). Attackers may trigger password recovery as part of account takeover. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate users who forgot their password. | [`password-recovery-requested.yml`](../rules/initial-access/password-recovery-requested.yml) |
| **alert** | Console Login Password Spray From Single Source IP | [T1110.003](https://attack.mitre.org/techniques/T1110/003/) | `ConsoleLogin` | Detects a single source IP generating failed console logins for 3 or more distinct usernames within 1 hour, indicating password spraying. | `eventSource`, `responseElements.ConsoleLogin`, `userIdentity.userName`, `sourceIPAddress`, `userIdentity.arn`, `userAgent`, `awsRegion` | Correlation: ≥3 distinct `userIdentity.userName` / 1h per `sourceIPAddress`. Level: **high**. | [`password-spray-console-login.yml`](../rules/initial-access/password-spray-console-login.yml) |
| **alert** | Root Account API Usage | [T1078.004](https://attack.mitre.org/techniques/T1078/004/) | userIdentity.type=Root | Detects any API activity performed by the account root user (outside of console sign-in). Best practice is to never use root for day-to-day API actions, so… | `userIdentity.type`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Rare account/billing operations that can only be performed by root. | [`root-account-api-usage.yml`](../rules/initial-access/root-account-api-usage.yml) |
| **alert** | Root Account Console Login | [T1078.004](https://attack.mitre.org/techniques/T1078/004/) | `ConsoleLogin` | Detects an interactive console sign-in by the AWS account root user. Root login should be extremely rare and reserved for tasks that require it; unexpected… | `eventSource`, `userIdentity.type`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Pre-approved break-glass or account-level maintenance that genuinely requires root. | [`root-console-login.yml`](../rules/initial-access/root-console-login.yml) |

## Execution

ATT&CK tactic: [TA0002](https://attack.mitre.org/tactics/TA0002/) — 5 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| hunt | ECS RunTask / Container Deployment | [T1610](https://attack.mitre.org/techniques/T1610/), [T1648](https://attack.mitre.org/techniques/T1648/) | `RunTask`, `StartTask` | Detects launching of ECS tasks (RunTask/StartTask). Adversaries deploy containers to execute code or mine cryptocurrency. Baseline expected task definitions… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Normal application deployments and autoscaling. | [`ecs-run-task.yml`](../rules/execution/ecs-run-task.yml) |
| hunt | Lambda Function Invocation via API | [T1648](https://attack.mitre.org/techniques/T1648/) | `Invoke` | Detects invocation of Lambda functions via the API (Invoke). Very common in normal operation; high-signal only for sensitive functions or unusual principals.… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Application traffic and scheduled invocations are expected. Scope to sensitive functions or unusual callers. | [`lambda-invoke.yml`](../rules/execution/lambda-invoke.yml) |
| hunt | SSM Document Created or Updated | [T1651](https://attack.mitre.org/techniques/T1651/) | `CreateDocument`, `UpdateDocument` | Detects creation or modification of SSM documents (CreateDocument/UpdateDocument). Malicious documents can be used to run code across many instances. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Infrastructure-as-code and automation teams authoring SSM documents. | [`ssm-create-document.yml`](../rules/execution/ssm-create-document.yml) |
| hunt | SSM SendCommand Execution on EC2 | [T1651](https://attack.mitre.org/techniques/T1651/) | `SendCommand` | Detects use of SSM SendCommand, which runs commands on managed EC2 instances (remote code execution path). Attackers use this for execution and lateral… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Patch/automation pipelines and the AWS SSM service role. Allowlist known automation principals. | [`ssm-send-command.yml`](../rules/execution/ssm-send-command.yml) |
| hunt | SSM Session Manager StartSession | [T1651](https://attack.mitre.org/techniques/T1651/), [T1021.007](https://attack.mitre.org/techniques/T1021/007/) | `StartSession` | Detects interactive Session Manager sessions to EC2 instances (StartSession). Provides shell access without SSH/RDP and is a common hands-on-keyboard and… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Administrators legitimately using Session Manager. Baseline per principal/instance. | [`ssm-start-session.yml`](../rules/execution/ssm-start-session.yml) |

## Persistence

ATT&CK tactic: [TA0003](https://attack.mitre.org/tactics/TA0003/) — 13 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| hunt | EC2 Key Pair Created or Imported | [T1098](https://attack.mitre.org/techniques/T1098/) | `CreateKeyPair`, `ImportKeyPair` | Detects creation or import of EC2 key pairs (CreateKeyPair/ImportKeyPair). Attackers add their own key material to retain SSH access to instances. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Normal instance provisioning workflows. | [`ec2-create-key-pair.yml`](../rules/persistence/ec2-create-key-pair.yml) |
| hunt | EC2 AMI Registered | [T1525](https://attack.mitre.org/techniques/T1525/) | `RegisterImage` | Detects registration of a new AMI (RegisterImage). A backdoored AMI used as a launch template provides durable persistence across new instances. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Image bakery/golden-AMI pipelines. | [`ec2-register-image.yml`](../rules/persistence/ec2-register-image.yml) |
| hunt | ECR Container Image Pushed | [T1525](https://attack.mitre.org/techniques/T1525/) | `PutImage` | Detects pushing an image to ECR (PutImage). Adversaries implant backdoored images so that future deployments run their code. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: CI/CD pipelines pushing images. Scope to sensitive repositories or unusual principals. | [`ecr-put-image.yml`](../rules/persistence/ecr-put-image.yml) |
| hunt | IAM Managed Policy Attached to User | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `AttachUserPolicy` | Detects attachment of a managed policy to an IAM user (AttachUserPolicy). Used to grant additional permissions for persistence or privilege escalation. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Routine permission grants by identity/platform teams. | [`iam-attach-user-policy.yml`](../rules/persistence/iam-attach-user-policy.yml) |
| hunt | IAM Access Key Created | [T1098.001](https://attack.mitre.org/techniques/T1098/001/) | `CreateAccessKey` | Detects creation of an IAM access key (CreateAccessKey). High-signal when the key is created for a DIFFERENT user than the caller (requestParameters.userName… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Users legitimately rotating their own access keys.; Automated key-rotation tooling. | [`iam-create-access-key.yml`](../rules/persistence/iam-create-access-key.yml) |
| **alert** | IAM Login Profile Created | [T1098](https://attack.mitre.org/techniques/T1098/), [T1136.003](https://attack.mitre.org/techniques/T1136/003/) | `CreateLoginProfile` | Detects creation of a console login profile/password for an IAM user (CreateLoginProfile). Granting console access to a previously API-only user is a common… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Legitimate onboarding granting console access to a user. | [`iam-create-login-profile.yml`](../rules/persistence/iam-create-login-profile.yml) |
| hunt | IAM Service-Specific Credential Created | [T1098.001](https://attack.mitre.org/techniques/T1098/001/) | `CreateServiceSpecificCredential` | Detects creation of service-specific credentials (CreateServiceSpecificCredential), e.g. for CodeCommit. These are an alternative long-lived credential an… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Developers provisioning CodeCommit/HTTPS Git credentials. | [`iam-create-service-specific-credential.yml`](../rules/persistence/iam-create-service-specific-credential.yml) |
| hunt | IAM User Created | [T1136.003](https://attack.mitre.org/techniques/T1136/003/) | `CreateUser` | Detects creation of a new IAM user (CreateUser). Adversaries create users to maintain persistent access independent of compromised credentials. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Provisioning of new service or human users by identity/platform teams. | [`iam-create-user.yml`](../rules/persistence/iam-create-user.yml) |
| hunt | Virtual MFA Device Created | [T1556.006](https://attack.mitre.org/techniques/T1556/006/) | `CreateVirtualMFADevice` | Detects creation of a virtual MFA device (CreateVirtualMFADevice). On its own benign, but combined with EnableMFADevice against another user it indicates an… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Normal MFA enrollment by users. | [`iam-create-virtual-mfa-device.yml`](../rules/persistence/iam-create-virtual-mfa-device.yml) |
| hunt | MFA Device Enabled for User | [T1556.006](https://attack.mitre.org/techniques/T1556/006/) | `EnableMFADevice` | Detects enabling an MFA device for an IAM user (EnableMFADevice). High-signal when an attacker enrolls their own MFA device onto a compromised account; verify… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate MFA enrollment during onboarding. | [`iam-enable-mfa-device.yml`](../rules/persistence/iam-enable-mfa-device.yml) |
| hunt | IAM Inline Policy Added to User | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `PutUserPolicy` | Detects addition of an inline policy to an IAM user (PutUserPolicy). Inline policies are easy to overlook in reviews, making them attractive for stealthy… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate fine-grained permission grants. | [`iam-put-user-policy.yml`](../rules/persistence/iam-put-user-policy.yml) |
| **alert** | IAM Login Profile Updated (Password Reset) | [T1098](https://attack.mitre.org/techniques/T1098/) | `UpdateLoginProfile` | Detects modification of an IAM user's console password (UpdateLoginProfile). Attackers reset passwords to hijack accounts; high-signal when performed against… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Help-desk password resets and self-service password changes. | [`iam-update-login-profile.yml`](../rules/persistence/iam-update-login-profile.yml) |
| hunt | Lambda Function Created | [T1098](https://attack.mitre.org/techniques/T1098/), [T1648](https://attack.mitre.org/techniques/T1648/) | `CreateFunction20150331`, `CreateFunction` | Detects creation of a Lambda function (logged as CreateFunction20150331). Attackers deploy functions as backdoors or to trigger on events. Note the versioned… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Normal application/IaC deployments of functions. | [`lambda-create-function.yml`](../rules/persistence/lambda-create-function.yml) |

## Privilege Escalation

ATT&CK tactic: [TA0004](https://attack.mitre.org/tactics/TA0004/) — 11 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| hunt | IAM Role Added to Instance Profile | [T1098](https://attack.mitre.org/techniques/T1098/) | `AddRoleToInstanceProfile`, `AssociateIamInstanceProfile` | Detects associating an IAM role with an instance profile or attaching that profile to an instance (AddRoleToInstanceProfile/AssociateIamInstanceProfile). Used… | `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Standard EC2 provisioning assigning instance roles. | [`iam-add-role-to-instance-profile.yml`](../rules/privilege-escalation/iam-add-role-to-instance-profile.yml) |
| hunt | IAM User Added to Group | [T1098](https://attack.mitre.org/techniques/T1098/) | `AddUserToGroup` | Detects adding an IAM user to a group (AddUserToGroup). Adding a controlled user to a privileged group is a direct escalation path. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Routine group membership management. | [`iam-add-user-to-group.yml`](../rules/privilege-escalation/iam-add-user-to-group.yml) |
| **alert** | IAM Administrator/Wildcard Policy Attached | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `AttachUserPolicy`, `AttachRolePolicy`, `AttachGroupPolicy` | Detects attachment of the AWS managed AdministratorAccess policy to a user, role, or group. Matches the well-known admin policy ARN; custom wildcard (Action:*… | `eventSource`, `requestParameters.policyArn`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Deliberate granting of administrator access (should be rare and reviewed). | [`iam-attach-administrator-policy.yml`](../rules/privilege-escalation/iam-attach-administrator-policy.yml) |
| hunt | IAM Managed Policy Attached to Role | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `AttachRolePolicy` | Detects attachment of a managed policy to an IAM role (AttachRolePolicy). A common privilege-escalation step, especially when the role is assumable by the… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Routine role permission changes by platform teams. | [`iam-attach-role-policy.yml`](../rules/privilege-escalation/iam-attach-role-policy.yml) |
| hunt | IAM New Managed Policy Version Created | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `CreatePolicyVersion` | Detects creation of a new version of a customer-managed policy (CreatePolicyVersion). Combined with SetDefaultPolicyVersion this can quietly escalate the… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate policy updates. | [`iam-create-policy-version.yml`](../rules/privilege-escalation/iam-create-policy-version.yml) |
| hunt | IAM Role Created | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `CreateRole` | Detects creation of a new IAM role (CreateRole). Attackers create roles (often with permissive trust) as a persistence and escalation foothold. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Routine role creation by platform/IaC. | [`iam-create-role.yml`](../rules/privilege-escalation/iam-create-role.yml) |
| hunt | iam:PassRole to New Compute Resource | [T1098](https://attack.mitre.org/techniques/T1098/) | `RunInstances`, `CreateFunction20150331`, `CreateFunction` | Detects creation of compute resources that receive an IAM role (RunInstances/CreateFunction/CreateInstanceProfile etc.). NOTE: there is NO standalone… | `requestParameters.iamInstanceProfile`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Normal provisioning that assigns instance/function roles. Correlate the passed role's privileges with the… | [`iam-pass-role-to-new-resource.yml`](../rules/privilege-escalation/iam-pass-role-to-new-resource.yml) |
| hunt | IAM Policy Attached/Added to Group | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `AttachGroupPolicy`, `PutGroupPolicy` | Detects attaching a managed policy or adding an inline policy to an IAM group (AttachGroupPolicy/PutGroupPolicy), which escalates permissions for all members. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate group permission management. | [`iam-put-group-policy.yml`](../rules/privilege-escalation/iam-put-group-policy.yml) |
| hunt | IAM Inline Policy Added to Role | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `PutRolePolicy` | Detects addition of an inline policy to an IAM role (PutRolePolicy). Used to silently widen a role's permissions. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate fine-grained role permission grants. | [`iam-put-role-policy.yml`](../rules/privilege-escalation/iam-put-role-policy.yml) |
| hunt | IAM Default Policy Version Changed | [T1098.003](https://attack.mitre.org/techniques/T1098/003/) | `SetDefaultPolicyVersion` | Detects changing the default version of a managed policy (SetDefaultPolicyVersion). Attackers can switch the active policy version to one granting more… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate rollbacks/updates of managed policies. | [`iam-set-default-policy-version.yml`](../rules/privilege-escalation/iam-set-default-policy-version.yml) |
| **alert** | IAM Role Trust Policy Modified | [T1098](https://attack.mitre.org/techniques/T1098/), [T1548](https://attack.mitre.org/techniques/T1548/) | `UpdateAssumeRolePolicy` | Detects modification of a role's trust policy (UpdateAssumeRolePolicy). Attackers alter trust relationships to allow themselves (or an external account) to… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Legitimate changes to role trust by platform/identity teams. Investigate when the new principal is an… | [`iam-update-assume-role-policy.yml`](../rules/privilege-escalation/iam-update-assume-role-policy.yml) |

## Defense Evasion

ATT&CK tactic: [TA0005](https://attack.mitre.org/tactics/TA0005/) — 14 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| **alert** | CloudTrail Trail Deleted | [T1562.008](https://attack.mitre.org/techniques/T1562/008/) | `DeleteTrail` | Detects deletion of a CloudTrail trail (DeleteTrail), removing the audit trail entirely. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Decommissioning of a trail as part of an approved change. | [`cloudtrail-delete-trail.yml`](../rules/defense-evasion/cloudtrail-delete-trail.yml) |
| hunt | CloudTrail Event Selectors Modified | [T1562.008](https://attack.mitre.org/techniques/T1562/008/) | `PutEventSelectors` | Detects changes to CloudTrail event selectors (PutEventSelectors), which can be used to stop recording specific event categories (e.g. management or S3/Lambda… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate tuning of which events are captured. | [`cloudtrail-put-event-selectors.yml`](../rules/defense-evasion/cloudtrail-put-event-selectors.yml) |
| **alert** | CloudTrail Logging Stopped | [T1562.008](https://attack.mitre.org/techniques/T1562/008/) | `StopLogging` | Detects StopLogging on a CloudTrail trail, which halts delivery of management/data events - a direct attempt to blind defenders. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Rare, planned maintenance on a trail (should be change-controlled). | [`cloudtrail-stop-logging.yml`](../rules/defense-evasion/cloudtrail-stop-logging.yml) |
| **alert** | CloudTrail Trail Reconfigured | [T1562.008](https://attack.mitre.org/techniques/T1562/008/) | `UpdateTrail` | Detects modification of a CloudTrail trail (UpdateTrail), e.g. disabling multi-region logging, global service events, or log file validation to reduce… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Legitimate trail configuration changes (review the new settings). | [`cloudtrail-update-trail.yml`](../rules/defense-evasion/cloudtrail-update-trail.yml) |
| hunt | CloudWatch Alarms Deleted | [T1562.001](https://attack.mitre.org/techniques/T1562/001/) | `DeleteAlarms` | Detects deletion of CloudWatch alarms (DeleteAlarms), which can suppress alerting on malicious activity. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Routine cleanup of unused alarms. | [`cloudwatch-delete-alarms.yml`](../rules/defense-evasion/cloudwatch-delete-alarms.yml) |
| hunt | CloudWatch Log Group or Stream Deleted | [T1562.008](https://attack.mitre.org/techniques/T1562/008/), [T1070](https://attack.mitre.org/techniques/T1070/) | `DeleteLogGroup`, `DeleteLogStream` | Detects deletion of CloudWatch Logs groups or streams (DeleteLogGroup/DeleteLogStream), destroying application/audit logs. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Lifecycle management of log groups. | [`cloudwatch-logs-deleted.yml`](../rules/defense-evasion/cloudwatch-logs-deleted.yml) |
| **alert** | Config Recording Disabled | [T1562.008](https://attack.mitre.org/techniques/T1562/008/) | `StopConfigurationRecorder`, `DeleteConfigurationRecorder`, `DeleteDeliveryChannel`, `DeleteConfigRule` | Detects disabling or deletion of AWS Config recording or delivery (StopConfigurationRecorder, DeleteConfigurationRecorder, DeleteDeliveryChannel,… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Approved changes to AWS Config setup. | [`config-disable-recorder.yml`](../rules/defense-evasion/config-disable-recorder.yml) |
| **alert** | GuardDuty Disabled or Tampered | [T1562.008](https://attack.mitre.org/techniques/T1562/008/) | `DeleteDetector`, `StopMonitoringMembers`, `DeleteMembers`, `DisassociateFromMasterAccount`, `UpdateDetector` | Detects actions that disable or weaken GuardDuty (DeleteDetector, StopMonitoringMembers, DeleteMembers, DisassociateFromMasterAccount, UpdateDetector,… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Planned GuardDuty administration in multi-account setups. | [`guardduty-disable.yml`](../rules/defense-evasion/guardduty-disable.yml) |
| **alert** | MFA Device Deactivated | [T1556.006](https://attack.mitre.org/techniques/T1556/006/), [T1531](https://attack.mitre.org/techniques/T1531/) | `DeactivateMFADevice` | Detects deactivation of an MFA device (DeactivateMFADevice). Removing MFA weakens authentication and can lock out or impersonate the victim; high-signal… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Legitimate MFA device replacement/offboarding. | [`iam-deactivate-mfa-device.yml`](../rules/defense-evasion/iam-deactivate-mfa-device.yml) |
| **alert** | Account Removed From Organization | [T1562](https://attack.mitre.org/techniques/T1562/), [T1578](https://attack.mitre.org/techniques/T1578/) | `LeaveOrganization`, `RemoveAccountFromOrganization` | Detects an account leaving or being removed from an AWS Organization (LeaveOrganization/RemoveAccountFromOrganization), which can detach it from centralized… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Planned organizational restructuring. | [`organizations-leave-or-remove.yml`](../rules/defense-evasion/organizations-leave-or-remove.yml) |
| **alert** | S3 Public Access Block Removed | [T1562](https://attack.mitre.org/techniques/T1562/), [T1578.005](https://attack.mitre.org/techniques/T1578/005/) | `DeleteAccountPublicAccessBlock`, `DeletePublicAccessBlock`, `PutAccountPublicAccessBlock`, `PutPublicAccessBlock` | Detects removal or weakening of S3 Block Public Access (DeleteAccountPublicAccessBlock, DeletePublicAccessBlock,… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Deliberate, reviewed changes to public-access settings (rare). | [`s3-disable-public-access-block.yml`](../rules/defense-evasion/s3-disable-public-access-block.yml) |
| hunt | Security Hub Disabled or Weakened | [T1562.008](https://attack.mitre.org/techniques/T1562/008/) | `DisableSecurityHub`, `BatchDisableStandards`, `DeleteInsight` | Detects disabling Security Hub or its standards/findings (DisableSecurityHub, BatchDisableStandards, DeleteInsight, UpdateStandardsControl to disable controls). | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Planned Security Hub administration. | [`securityhub-disable.yml`](../rules/defense-evasion/securityhub-disable.yml) |
| **alert** | VPC Flow Logs Deleted | [T1562.008](https://attack.mitre.org/techniques/T1562/008/) | `DeleteFlowLogs` | Detects deletion of VPC flow logs (DeleteFlowLogs), removing network-level visibility. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Decommissioning of flow logs during network changes. | [`vpc-delete-flow-logs.yml`](../rules/defense-evasion/vpc-delete-flow-logs.yml) |
| hunt | WAF Rule or WebACL Deleted | [T1562.001](https://attack.mitre.org/techniques/T1562/001/) | `DeleteWebACL`, `DeleteRuleGroup`, `DeleteIPSet` | Detects deletion of AWS WAF protections (DeleteWebACL/DeleteRuleGroup/DeleteIPSet), removing application-layer defenses. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate WAF reconfiguration. | [`waf-delete.yml`](../rules/defense-evasion/waf-delete.yml) |

## Credential Access

ATT&CK tactic: [TA0006](https://attack.mitre.org/tactics/TA0006/) — 8 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| **alert** | EC2 Windows Password Data Retrieved | [T1552](https://attack.mitre.org/techniques/T1552/) | `GetPasswordData` | Detects retrieval of the encrypted local Administrator password for a Windows EC2 instance (GetPasswordData). Used to obtain credentials for RDP/lateral… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Provisioning workflows that retrieve the initial Windows password. | [`ec2-get-password-data.yml`](../rules/credential-access/ec2-get-password-data.yml) |
| hunt | KMS Decrypt Operations | [T1555.006](https://attack.mitre.org/techniques/T1555/006/) | `Decrypt` | Detects KMS Decrypt calls (a data event). Bulk decryption by an unusual principal can indicate access to protected secrets/data. Requires KMS data events to be… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Applications decrypting data as part of normal operation. Baseline per principal/key. | [`kms-decrypt.yml`](../rules/credential-access/kms-decrypt.yml) |
| hunt | RDS Database Log Downloaded | [T1552](https://attack.mitre.org/techniques/T1552/) | `DownloadDBLogFilePortion`, `DownloadCompleteDBLogFile` | Detects downloading of RDS database log files (DownloadDBLogFilePortion/DownloadCompleteDBLogFile), which may contain credentials or sensitive query data. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: DBAs/tooling retrieving logs for troubleshooting. | [`rds-download-db-log.yml`](../rules/credential-access/rds-download-db-log.yml) |
| hunt | Secrets Manager Batch Secret Retrieval | [T1555.006](https://attack.mitre.org/techniques/T1555/006/) | `BatchGetSecretValue` | Detects BatchGetSecretValue, which returns multiple secrets in one call - an efficient credential-harvesting primitive. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Tooling that legitimately reads many secrets at once. | [`secretsmanager-batch-get-secret-value.yml`](../rules/credential-access/secretsmanager-batch-get-secret-value.yml) |
| hunt | Secrets Manager Secret Retrieved | [T1555.006](https://attack.mitre.org/techniques/T1555/006/) | `GetSecretValue` | Detects retrieval of a secret value (GetSecretValue). Single calls are normal; bulk retrieval across many secrets by one principal indicates credential… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Applications and pipelines reading secrets at runtime. Baseline per principal. | [`secretsmanager-get-secret-value.yml`](../rules/credential-access/secretsmanager-get-secret-value.yml) |
| hunt | SSM Parameter Retrieved With Decryption | [T1555.006](https://attack.mitre.org/techniques/T1555/006/) | `GetParameter`, `GetParameters`, `GetParametersByPath` | Detects retrieval of SSM Parameter Store parameters with decryption (GetParameter/GetParameters/GetParametersByPath with withDecryption=true), commonly used to… | `eventSource`, `requestParameters.withDecryption`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Applications reading their own SecureString parameters at runtime. | [`ssm-get-parameter-decrypt.yml`](../rules/credential-access/ssm-get-parameter-decrypt.yml) |
| hunt | STS GetFederationToken | [T1528](https://attack.mitre.org/techniques/T1528/) | `GetFederationToken` | Detects GetFederationToken, which creates temporary federated credentials. Attackers abuse it to mint scoped tokens for persistence or to share access. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate federation brokers and identity tooling. | [`sts-get-federation-token.yml`](../rules/credential-access/sts-get-federation-token.yml) |
| hunt | STS GetSessionToken | [T1528](https://attack.mitre.org/techniques/T1528/), [T1550](https://attack.mitre.org/techniques/T1550/) | `GetSessionToken` | Detects GetSessionToken calls that mint temporary credentials. Noisy on its own; useful when correlated with unusual principals or downstream abuse. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Normal use of temporary credentials by users/tools, especially with MFA. | [`sts-get-session-token.yml`](../rules/credential-access/sts-get-session-token.yml) |

## Discovery

ATT&CK tactic: [TA0007](https://attack.mitre.org/tactics/TA0007/) — 9 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| hunt | Permission Enumeration via Access Denied Burst | [T1087.004](https://attack.mitre.org/techniques/T1087/004/), [T1069.003](https://attack.mitre.org/techniques/T1069/003/) | errorCode=AccessDenied | Detects a single principal generating AccessDenied errors across 3 or more distinct API actions within 30 minutes - characteristic of permission… | `errorCode`, `eventName`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Correlation: ≥3 distinct `eventName` / 30m per `userIdentity.arn`. Level: **medium**. | [`access-denied-burst.yml`](../rules/discovery/access-denied-burst.yml) |
| hunt | Region and Account Enumeration | [T1580](https://attack.mitre.org/techniques/T1580/) | `DescribeRegions`, `ListRegions`, `GetRegions` | Detects enumeration of enabled regions/account metadata (DescribeRegions/ListRegions/GetRegions). Attackers map enabled regions to find under-monitored areas. | `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **informational**. FP: Common in tooling and console navigation. | [`account-describe-regions.yml`](../rules/discovery/account-describe-regions.yml) |
| hunt | EC2 Infrastructure Enumeration | [T1580](https://attack.mitre.org/techniques/T1580/) | `DescribeInstances`, `DescribeSecurityGroups`, `DescribeVpcs`, `DescribeSnapshots`, `DescribeImages`, `DescribeAddresses` | Detects bulk EC2 describe calls (DescribeInstances/DescribeSecurityGroups/DescribeVpcs/DescribeSnapshots/DescribeImages). High volume/variety from one… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Monitoring, inventory, and cost tools enumerate EC2 regularly. Apply thresholds. | [`ec2-describe-enumeration.yml`](../rules/discovery/ec2-describe-enumeration.yml) |
| hunt | IAM Principal Enumeration | [T1087.004](https://attack.mitre.org/techniques/T1087/004/) | `ListUsers`, `ListRoles`, `ListGroups`, `ListPolicies`, `GetAccountSummary` | Detects enumeration of IAM principals (ListUsers/ListRoles/ListGroups/ListPolicies/GetAccountSummary). Common in recon after gaining initial access; correlate… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Inventory, audit, and IaC tooling enumerating IAM. | [`iam-enumerate-principals.yml`](../rules/discovery/iam-enumerate-principals.yml) |
| hunt | IAM Authorization Details Enumerated | [T1087.004](https://attack.mitre.org/techniques/T1087/004/) | `GetAccountAuthorizationDetails` | Detects GetAccountAuthorizationDetails, which dumps all users, groups, roles, and their policies in one call - a classic IAM reconnaissance primitive. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: IAM auditing/inventory tooling (e.g. access analyzers, posture scanners). | [`iam-get-account-authorization-details.yml`](../rules/discovery/iam-get-account-authorization-details.yml) |
| hunt | IAM Permission/Policy Enumeration | [T1069.003](https://attack.mitre.org/techniques/T1069/003/) | `ListAttachedUserPolicies`, `ListAttachedRolePolicies`, `ListGroupsForUser`, `SimulatePrincipalPolicy` | Detects enumeration of effective permissions (ListAttachedUserPolicies/ListAttachedRolePolicies/ListGroupsForUser/SimulatePrincipalPolicy), used to map… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Access reviews and least-privilege tooling. | [`iam-permission-enumeration.yml`](../rules/discovery/iam-permission-enumeration.yml) |
| hunt | Organizations Structure Enumeration | [T1526](https://attack.mitre.org/techniques/T1526/), [T1538](https://attack.mitre.org/techniques/T1538/) | `DescribeOrganization`, `ListAccounts`, `ListOrganizationalUnitsForParent` | Detects enumeration of the AWS Organization (DescribeOrganization/ListAccounts/ListOrganizationalUnitsForParent), revealing the multi-account layout for… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Governance, billing, and landing-zone tooling. | [`organizations-describe.yml`](../rules/discovery/organizations-describe.yml) |
| hunt | S3 Bucket Enumeration | [T1580](https://attack.mitre.org/techniques/T1580/), [T1530](https://attack.mitre.org/techniques/T1530/) | `ListBuckets` | Detects ListBuckets, which enumerates all buckets in the account - a common precursor to data discovery and exfiltration. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Backup, inventory, and security tooling list buckets routinely. | [`s3-list-buckets.yml`](../rules/discovery/s3-list-buckets.yml) |
| hunt | STS GetCallerIdentity (whoami) | [T1087.004](https://attack.mitre.org/techniques/T1087/004/), [T1580](https://attack.mitre.org/techniques/T1580/) | `GetCallerIdentity` | Detects GetCallerIdentity, the cloud equivalent of 'whoami', frequently the first call an attacker makes with fresh credentials. Extremely common in… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Ubiquitous in SDKs, CLIs, and CI/CD. Use only as enrichment or correlate with other recon. | [`sts-get-caller-identity.yml`](../rules/discovery/sts-get-caller-identity.yml) |

## Lateral Movement

ATT&CK tactic: [TA0008](https://attack.mitre.org/tactics/TA0008/) — 4 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| hunt | EC2 Instance Connect SSH Key Pushed | [T1021.007](https://attack.mitre.org/techniques/T1021/007/) | `SendSSHPublicKey` | Detects pushing a temporary SSH public key to an instance via EC2 Instance Connect (SendSSHPublicKey), enabling SSH access for lateral movement. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Administrators using EC2 Instance Connect for legitimate access. | [`ec2-instance-connect-send-ssh-key.yml`](../rules/lateral-movement/ec2-instance-connect-send-ssh-key.yml) |
| hunt | EC2 Serial Console Access | [T1021.007](https://attack.mitre.org/techniques/T1021/007/) | `SendSerialConsoleSSHPublicKey`, `StartSerialConsoleSession` | Detects EC2 serial console access (SendSerialConsoleSSHPublicKey/StartSerialConsoleSession), which provides low-level instance access that can bypass network… | `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Break-glass instance troubleshooting (serial console is often disabled by default). | [`ec2-serial-console-ssh-key.yml`](../rules/lateral-movement/ec2-serial-console-ssh-key.yml) |
| hunt | STS Cross-Account Role Assumption | [T1021.007](https://attack.mitre.org/techniques/T1021/007/) | `AssumeRole`, `AssumeRoleWithSAML`, `AssumeRoleWithWebIdentity` | Detects AssumeRole/AssumeRoleWithSAML/AssumeRoleWithWebIdentity used to move between accounts. Inspect whether the requested role's account differs from the… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate cross-account automation and federation. Allowlist known trust pairs. | [`sts-assume-role-cross-account.yml`](../rules/lateral-movement/sts-assume-role-cross-account.yml) |
| hunt | STS AssumeRole | [T1021.007](https://attack.mitre.org/techniques/T1021/007/), [T1078.004](https://attack.mitre.org/techniques/T1078/004/) | `AssumeRole` | Detects role assumption (AssumeRole). Ubiquitous in normal operation; high-signal when chained, cross-account, or from unusual principals/IPs. Use as a… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Pervasive in normal multi-account and service-to-service operation. | [`sts-assume-role.yml`](../rules/lateral-movement/sts-assume-role.yml) |

## Collection

ATT&CK tactic: [TA0009](https://attack.mitre.org/tactics/TA0009/) — 5 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| hunt | DynamoDB Table Exported | [T1530](https://attack.mitre.org/techniques/T1530/) | `ExportTableToPointInTime` | Detects export of a DynamoDB table to S3 (ExportTableToPointInTime), a bulk data-collection technique. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate analytics/backup exports. | [`dynamodb-export-table.yml`](../rules/collection/dynamodb-export-table.yml) |
| hunt | EBS Snapshot Created | [T1530](https://attack.mitre.org/techniques/T1530/) | `CreateSnapshot`, `CreateSnapshots` | Detects creation of EBS snapshots (CreateSnapshot/CreateSnapshots). Snapshotting volumes is a common way to collect disk contents prior to… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Backup automation and routine volume snapshots. | [`ec2-create-snapshot.yml`](../rules/collection/ec2-create-snapshot.yml) |
| hunt | RDS Snapshot Created | [T1530](https://attack.mitre.org/techniques/T1530/) | `CreateDBSnapshot`, `CreateDBClusterSnapshot` | Detects creation of RDS DB/cluster snapshots (CreateDBSnapshot/CreateDBClusterSnapshot). Attackers snapshot databases to collect data they can later… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Backup automation and DBA workflows. | [`rds-create-db-snapshot.yml`](../rules/collection/rds-create-db-snapshot.yml) |
| hunt | S3 Object Retrieval | [T1530](https://attack.mitre.org/techniques/T1530/) | `GetObject` | Detects S3 GetObject (a data event). Individually normal; mass retrieval by one principal indicates data staging/collection. Requires S3 data events to be… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Applications and users reading objects routinely. Baseline per principal/bucket. | [`s3-get-object.yml`](../rules/collection/s3-get-object.yml) |
| hunt | S3 Mass Object Listing | [T1530](https://attack.mitre.org/techniques/T1530/) | `ListObjects`, `ListObjectsV2` | Detects S3 ListObjects/ListObjectsV2 enumeration of bucket contents, a precursor to bulk download. Requires data events for object-level visibility; correlate… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **low**. FP: Backup, sync, and analytics jobs that list objects. | [`s3-mass-list-objects.yml`](../rules/collection/s3-mass-list-objects.yml) |

## Exfiltration

ATT&CK tactic: [TA0010](https://attack.mitre.org/tactics/TA0010/) — 7 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| hunt | DataSync Task Created | [T1537](https://attack.mitre.org/techniques/T1537/), [T1567](https://attack.mitre.org/techniques/T1567/) | `CreateTask`, `CreateLocationS3`, `CreateLocationObjectStorage` | Detects creation of DataSync tasks/locations (CreateTask/CreateLocationS3/CreateLocationObjectStorage), which can move large volumes of data to an… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate data-migration and backup jobs. | [`datasync-create-task.yml`](../rules/exfiltration/datasync-create-task.yml) |
| hunt | EC2 Instance/Image Export Task | [T1537](https://attack.mitre.org/techniques/T1537/) | `CreateInstanceExportTask`, `ExportImage` | Detects export of an instance or image to S3 (CreateInstanceExportTask/ExportImage), which can stage a full VM image for exfiltration. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate VM export/migration workflows. | [`ec2-create-instance-export-task.yml`](../rules/exfiltration/ec2-create-instance-export-task.yml) |
| **alert** | AMI Shared Externally | [T1537](https://attack.mitre.org/techniques/T1537/) | `ModifyImageAttribute` | Detects modification of AMI permissions (ModifyImageAttribute). Sharing/making an AMI public can leak baked-in data and enable restore in an attacker account.… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Deliberate AMI sharing with known accounts (allowlist). | [`ec2-modify-image-attribute.yml`](../rules/exfiltration/ec2-modify-image-attribute.yml) |
| **alert** | EBS Snapshot Shared Externally | [T1537](https://attack.mitre.org/techniques/T1537/) | `ModifySnapshotAttribute` | Detects modification of EBS snapshot permissions (ModifySnapshotAttribute). Sharing a snapshot with another AWS account or making it public lets an attacker… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Legitimate sharing of snapshots with known partner accounts (allowlist account IDs). | [`ec2-modify-snapshot-attribute.yml`](../rules/exfiltration/ec2-modify-snapshot-attribute.yml) |
| **alert** | RDS Snapshot Shared Externally | [T1537](https://attack.mitre.org/techniques/T1537/) | `ModifyDBSnapshotAttribute`, `ModifyDBClusterSnapshotAttribute` | Detects sharing of RDS snapshots (ModifyDBSnapshotAttribute/ModifyDBClusterSnapshotAttribute) with other accounts or all (restore=all). A primary… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Legitimate snapshot sharing with known partner accounts (allowlist). | [`rds-modify-db-snapshot-attribute.yml`](../rules/exfiltration/rds-modify-db-snapshot-attribute.yml) |
| **alert** | S3 Bucket ACL Changed to Public | [T1537](https://attack.mitre.org/techniques/T1537/) | `PutBucketAcl` | Detects changes to a bucket ACL (PutBucketAcl) that grant access to AllUsers/AuthenticatedUsers (public). Inspect requestParameters.AccessControlPolicy grants… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Rare legitimate public-content buckets (should use explicit, reviewed policy). | [`s3-put-bucket-acl-public.yml`](../rules/exfiltration/s3-put-bucket-acl-public.yml) |
| **alert** | S3 Bucket Policy Modified | [T1537](https://attack.mitre.org/techniques/T1537/) | `PutBucketPolicy` | Detects changes to an S3 bucket policy (PutBucketPolicy). Attackers grant access to an external principal/'*' to exfiltrate data. Inspect the policy for… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Legitimate bucket policy changes (review the granted principals). | [`s3-put-bucket-policy.yml`](../rules/exfiltration/s3-put-bucket-policy.yml) |

## Impact

ATT&CK tactic: [TA0040](https://attack.mitre.org/tactics/TA0040/) — 13 detection(s).

| Tier | Technique / Activity | ATT&CK ID | CloudTrail event(s) | What it indicates | Key fields to inspect | Detection notes | Sigma rule |
|---|---|---|---|---|---|---|---|
| **alert** | Backup Resources Deleted | [T1490](https://attack.mitre.org/techniques/T1490/) | `DeleteBackupVault`, `DeleteRecoveryPoint`, `DeleteBackupPlan` | Detects deletion of AWS Backup resources (DeleteBackupVault/DeleteRecoveryPoint/DeleteBackupPlan), inhibiting recovery from a destructive attack. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Backup lifecycle management (should be controlled). | [`backup-delete.yml`](../rules/impact/backup-delete.yml) |
| hunt | DynamoDB Table Deleted | [T1485](https://attack.mitre.org/techniques/T1485/) | `DeleteTable` | Detects deletion of a DynamoDB table (DeleteTable), a destructive data-loss action. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Decommissioning of unused tables. | [`dynamodb-delete-table.yml`](../rules/impact/dynamodb-delete-table.yml) |
| hunt | EBS Snapshot Deleted | [T1490](https://attack.mitre.org/techniques/T1490/) | `DeleteSnapshot` | Detects deletion of EBS snapshots (DeleteSnapshot), removing volume recovery points. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Snapshot lifecycle/retention cleanup. | [`ec2-delete-snapshot.yml`](../rules/impact/ec2-delete-snapshot.yml) |
| hunt | EC2 Large/GPU Instance Launched (Possible Cryptomining) | [T1496](https://attack.mitre.org/techniques/T1496/) | `RunInstances` | Detects launching of large or GPU EC2 instance types frequently abused for cryptomining (RunInstances with p/g/x/large families). Heuristic - tune the… | `eventSource`, `requestParameters.instanceType`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Legitimate ML/HPC/big-data workloads using GPU or large instances. Allowlist expected types/accounts. | [`ec2-run-instances-resource-hijacking.yml`](../rules/impact/ec2-run-instances-resource-hijacking.yml) |
| hunt | EC2 Instances Terminated or Stopped | [T1485](https://attack.mitre.org/techniques/T1485/), [T1496](https://attack.mitre.org/techniques/T1496/) | `TerminateInstances`, `StopInstances` | Detects termination/stopping of EC2 instances (TerminateInstances/StopInstances). Bulk operations can indicate destruction or disruption; correlate volume per… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Autoscaling and routine instance lifecycle management. | [`ec2-terminate-instances.yml`](../rules/impact/ec2-terminate-instances.yml) |
| hunt | IAM Account Access Removal | [T1531](https://attack.mitre.org/techniques/T1531/) | `DeleteUser`, `DeleteLoginProfile`, `DeleteAccessKey` | Detects removal of IAM access (DeleteUser/DeleteLoginProfile/DeleteAccessKey), which attackers use to lock out legitimate users/responders. High-signal against… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Offboarding and key cleanup. Correlate with HR/offboarding processes. | [`iam-delete-user-lockout.yml`](../rules/impact/iam-delete-user-lockout.yml) |
| **alert** | KMS Key Disabled or Policy Weakened | [T1486](https://attack.mitre.org/techniques/T1486/) | `DisableKey`, `DisableKeyRotation`, `PutKeyPolicy` | Detects disabling a KMS key or its rotation, or rewriting its policy (DisableKey/DisableKeyRotation/PutKeyPolicy). Can break decryption (denial) or grant the… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Legitimate key administration (review the change). | [`kms-disable-key.yml`](../rules/impact/kms-disable-key.yml) |
| **alert** | KMS Key Deletion Scheduled | [T1486](https://attack.mitre.org/techniques/T1486/) | `ScheduleKeyDeletion` | Detects scheduling deletion of a KMS key (ScheduleKeyDeletion). Destroying a key renders all data encrypted with it permanently unrecoverable - a… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Planned key retirement (should be rare and reviewed). | [`kms-schedule-key-deletion.yml`](../rules/impact/kms-schedule-key-deletion.yml) |
| **alert** | RDS Database Deleted | [T1485](https://attack.mitre.org/techniques/T1485/) | `DeleteDBInstance`, `DeleteDBCluster` | Detects deletion of RDS databases/clusters (DeleteDBInstance/DeleteDBCluster). Destructive; especially severe when SkipFinalSnapshot=true. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Planned decommissioning of databases. | [`rds-delete-db-instance.yml`](../rules/impact/rds-delete-db-instance.yml) |
| hunt | RDS Snapshot Deleted | [T1490](https://attack.mitre.org/techniques/T1490/) | `DeleteDBSnapshot`, `DeleteDBClusterSnapshot` | Detects deletion of RDS snapshots (DeleteDBSnapshot/DeleteDBClusterSnapshot), removing database recovery points. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Snapshot lifecycle/retention cleanup. | [`rds-delete-db-snapshot.yml`](../rules/impact/rds-delete-db-snapshot.yml) |
| **alert** | S3 Bucket Deleted | [T1485](https://attack.mitre.org/techniques/T1485/) | `DeleteBucket` | Detects deletion of an S3 bucket (DeleteBucket). Destructive action that can cause data loss/outage. | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **high**. FP: Decommissioning of unused buckets (should be change-controlled). | [`s3-delete-bucket.yml`](../rules/impact/s3-delete-bucket.yml) |
| hunt | S3 Object Deletion | [T1485](https://attack.mitre.org/techniques/T1485/) | `DeleteObject`, `DeleteObjects` | Detects deletion of S3 objects (DeleteObject/DeleteObjects), a data event. Mass deletion indicates destruction or ransomware. Requires S3 data events; apply… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Lifecycle and application cleanup. Baseline per principal/bucket. | [`s3-delete-object.yml`](../rules/impact/s3-delete-object.yml) |
| hunt | S3 Bucket Versioning Suspended | [T1490](https://attack.mitre.org/techniques/T1490/) | `PutBucketVersioning` | Detects suspension of S3 bucket versioning (PutBucketVersioning). Disabling versioning removes the ability to recover overwritten/deleted objects - often a… | `eventSource`, `userIdentity.arn`, `sourceIPAddress`, `userAgent`, `awsRegion` | Level: **medium**. FP: Deliberate, reviewed versioning configuration changes. | [`s3-suspend-bucket-versioning.yml`](../rules/impact/s3-suspend-bucket-versioning.yml) |
