{
  "type": "new_terms",
  "language": "kuery",
  "index": [
    "logs-aws.cloudtrail-*",
    "aws-cloudtrail-*"
  ],
  "name": "AWS first-seen: s3-get-object (behavioral)",
  "description": "Behavioral detection. Fires when (userIdentity.arn, requestParameters.bucketName) is new for this principal in the last 90 days. Source rule: rules/collection/s3-get-object.yml. Adjust field names to your Elastic CloudTrail mapping (e.g. aws.cloudtrail.* with the Fleet integration).",
  "query": "eventName:(\"GetObject\") and eventSource:\"s3.amazonaws.com\"",
  "new_terms_fields": [
    "userIdentity.arn",
    "requestParameters.bucketName"
  ],
  "history_window_start": "now-90d",
  "severity": "low",
  "risk_score": 21
}
