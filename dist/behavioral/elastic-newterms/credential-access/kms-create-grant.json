{
  "type": "new_terms",
  "language": "kuery",
  "index": [
    "logs-aws.cloudtrail-*",
    "aws-cloudtrail-*"
  ],
  "name": "AWS first-seen: kms-create-grant (behavioral)",
  "description": "Behavioral detection. Fires when (userIdentity.arn, requestParameters.keyId) is new for this principal in the last 90 days. Source rule: rules/credential-access/kms-create-grant.yml. Adjust field names to your Elastic CloudTrail mapping (e.g. aws.cloudtrail.* with the Fleet integration).",
  "query": "eventName:(\"CreateGrant\") and eventSource:\"kms.amazonaws.com\"",
  "new_terms_fields": [
    "userIdentity.arn",
    "requestParameters.keyId"
  ],
  "history_window_start": "now-90d",
  "severity": "medium",
  "risk_score": 47
}
