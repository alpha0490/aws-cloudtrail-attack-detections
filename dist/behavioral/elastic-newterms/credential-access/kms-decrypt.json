{
  "type": "new_terms",
  "language": "kuery",
  "index": [
    "logs-aws.cloudtrail-*",
    "aws-cloudtrail-*"
  ],
  "name": "AWS first-seen: kms-decrypt (behavioral)",
  "description": "Behavioral detection. Fires when (userIdentity.arn, sourceIPAddress) is new for this principal in the last 90 days. Source rule: rules/credential-access/kms-decrypt.yml. Adjust field names to your Elastic CloudTrail mapping (e.g. aws.cloudtrail.* with the Fleet integration).",
  "query": "eventName:(\"Decrypt\") and eventSource:\"kms.amazonaws.com\"",
  "new_terms_fields": [
    "userIdentity.arn",
    "sourceIPAddress"
  ],
  "history_window_start": "now-90d",
  "severity": "low",
  "risk_score": 21
}
