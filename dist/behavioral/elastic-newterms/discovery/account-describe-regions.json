{
  "type": "new_terms",
  "language": "kuery",
  "index": [
    "logs-aws.cloudtrail-*",
    "aws-cloudtrail-*"
  ],
  "name": "AWS first-seen: account-describe-regions (behavioral)",
  "description": "Behavioral detection. Fires when (userIdentity.arn, eventName) is new for this principal in the last 90 days. Source rule: rules/discovery/account-describe-regions.yml. Adjust field names to your Elastic CloudTrail mapping (e.g. aws.cloudtrail.* with the Fleet integration).",
  "query": "eventName:(\"DescribeRegions\" or \"ListRegions\" or \"GetRegions\")",
  "new_terms_fields": [
    "userIdentity.arn",
    "eventName"
  ],
  "history_window_start": "now-90d",
  "severity": "low",
  "risk_score": 21
}
