{
  "type": "new_terms",
  "language": "kuery",
  "index": [
    "logs-aws.cloudtrail-*",
    "aws-cloudtrail-*"
  ],
  "name": "AWS first-seen: ecs-run-task (behavioral)",
  "description": "Behavioral detection. Fires when (userIdentity.arn, requestParameters.taskDefinition) is new for this principal in the last 90 days. Source rule: rules/execution/ecs-run-task.yml. Adjust field names to your Elastic CloudTrail mapping (e.g. aws.cloudtrail.* with the Fleet integration).",
  "query": "eventName:(\"RunTask\" or \"StartTask\") and eventSource:\"ecs.amazonaws.com\"",
  "new_terms_fields": [
    "userIdentity.arn",
    "requestParameters.taskDefinition"
  ],
  "history_window_start": "now-90d",
  "severity": "low",
  "risk_score": 21
}
