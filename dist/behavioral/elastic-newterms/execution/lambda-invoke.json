{
  "type": "new_terms",
  "language": "kuery",
  "index": [
    "logs-aws.cloudtrail-*",
    "aws-cloudtrail-*"
  ],
  "name": "AWS first-seen: lambda-invoke (behavioral)",
  "description": "Behavioral detection. Fires when (userIdentity.arn, requestParameters.functionName) is new for this principal in the last 90 days. Source rule: rules/execution/lambda-invoke.yml. Adjust field names to your Elastic CloudTrail mapping (e.g. aws.cloudtrail.* with the Fleet integration).",
  "query": "eventName:(\"Invoke\") and eventSource:\"lambda.amazonaws.com\"",
  "new_terms_fields": [
    "userIdentity.arn",
    "requestParameters.functionName"
  ],
  "history_window_start": "now-90d",
  "severity": "low",
  "risk_score": 21
}
