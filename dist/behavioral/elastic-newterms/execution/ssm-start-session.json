{
  "type": "new_terms",
  "language": "kuery",
  "index": [
    "logs-aws.cloudtrail-*",
    "aws-cloudtrail-*"
  ],
  "name": "AWS first-seen: ssm-start-session (behavioral)",
  "description": "Behavioral detection. Fires when (userIdentity.arn, requestParameters.target) is new for this principal in the last 90 days. Source rule: rules/execution/ssm-start-session.yml. Adjust field names to your Elastic CloudTrail mapping (e.g. aws.cloudtrail.* with the Fleet integration).",
  "query": "eventName:(\"StartSession\") and eventSource:\"ssm.amazonaws.com\"",
  "new_terms_fields": [
    "userIdentity.arn",
    "requestParameters.target"
  ],
  "history_window_start": "now-90d",
  "severity": "medium",
  "risk_score": 47
}
