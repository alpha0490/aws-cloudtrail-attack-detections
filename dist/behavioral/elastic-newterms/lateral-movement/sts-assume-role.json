{
  "type": "new_terms",
  "language": "kuery",
  "index": [
    "logs-aws.cloudtrail-*",
    "aws-cloudtrail-*"
  ],
  "name": "AWS first-seen: sts-assume-role (behavioral)",
  "description": "Behavioral detection. Fires when (userIdentity.arn, requestParameters.roleArn) is new for this principal in the last 90 days. Source rule: rules/lateral-movement/sts-assume-role.yml. Adjust field names to your Elastic CloudTrail mapping (e.g. aws.cloudtrail.* with the Fleet integration).",
  "query": "eventName:(\"AssumeRole\") and eventSource:\"sts.amazonaws.com\"",
  "new_terms_fields": [
    "userIdentity.arn",
    "requestParameters.roleArn"
  ],
  "history_window_start": "now-90d",
  "severity": "low",
  "risk_score": 21
}
