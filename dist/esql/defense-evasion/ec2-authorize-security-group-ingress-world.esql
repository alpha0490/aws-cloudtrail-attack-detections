from * metadata _id, _index, _version | where eventName=="AuthorizeSecurityGroupIngress" and eventSource=="ec2.amazonaws.com" and requestParameters.ipPermissions.items.ipRanges.items.cidrIp=="0.0.0.0/0"
