from * metadata _id, _index, _version | where (eventName in ("AttachUserPolicy", "AttachRolePolicy", "AttachGroupPolicy")) and eventSource=="iam.amazonaws.com" and requestParameters.policyArn like "*AdministratorAccess*"
