eventName="ConsoleLogin" eventSource="signin.amazonaws.com" responseElements.ConsoleLogin="Failure"

| bin _time span=1h
| stats dc(userIdentity.userName) as value_count by _time sourceIPAddress

| search value_count >= 3
