# Pinned toolchain so generated artifacts (especially dist/) are reproducible across
# machines and in CI. Install with:  pip install -r requirements-dev.txt
sigma-cli==1.0.6
pySigma==0.11.23
PyYAML>=6.0,<7

# SIEM backends used by scripts/build_dist.py (sigma convert)
pySigma-backend-splunk==1.1.3
pySigma-backend-elasticsearch==1.1.6
pySigma-backend-kusto==0.4.5
pysigma-backend-crowdstrike==2.0.1
